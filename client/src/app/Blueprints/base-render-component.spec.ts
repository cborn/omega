import { BaseRenderComponent } from './base-render-component';

describe('BaseRenderComponent', () => {
  it('should create an instance', () => {
    expect(new BaseRenderComponent()).toBeTruthy();
  });
});
