import { CommentAddingDialog } from './comment-adding-dialog';

describe('CommentAddingDialog', () => {
  it('should create an instance', () => {
    expect(new CommentAddingDialog()).toBeTruthy();
  });
});
