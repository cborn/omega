import { ClozeFormatPipe } from './cloze-format.pipe';

describe('ClozeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ClozeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
