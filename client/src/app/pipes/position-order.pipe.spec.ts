import { PositionOrderPipe } from './position-order.pipe';

describe('PositionOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new PositionOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
