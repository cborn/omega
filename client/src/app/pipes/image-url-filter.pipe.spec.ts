import { ImageUrlFilterPipe } from './image-url-filter.pipe';

describe('ImageUrlFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageUrlFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
