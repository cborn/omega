import { CommentOrderPipe } from './comment-order.pipe';

describe('CommentOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new CommentOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
